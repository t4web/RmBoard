define(
    'TasksView',
    ["backbone"],
    function(Backbone) {
        'use strict';

        var TasksView = Backbone.View.extend({

            render: function(tasks) {
                //console.log(tasks);
            }

        });

        return TasksView;
    }
);
